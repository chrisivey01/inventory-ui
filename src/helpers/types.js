export const itemType = (item) => {
    item.type = "item_standard";
    return item;
};

export const weaponType = (item) => {
    item.type = "item_weapon";
    return item;
};

export const accountType = (item) => {
    item.type = "item_account";
    return item;
};
